* `$ npm run build-dev`
    * webpack.dev.configを用いてビルド

* `$ npm run build-prod`
    * webpack.prod.configを用いてビルド

* `$ npm run dev-server`
    * webpack.dev.configを用いてビルド後、開発サーバ上にデプロイ。hotreload.
